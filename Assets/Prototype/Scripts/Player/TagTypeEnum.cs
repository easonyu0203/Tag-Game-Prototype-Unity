namespace Player{

    /// <summary>
    ///  The type of player in game, where catching other(Ghost) or be catched(Human)
    /// </summary>
    public enum TagTypeEnum
    {
        None,
        Human,
        Ghost,
    }

}
